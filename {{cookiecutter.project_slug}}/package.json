{
  "name": "{{ cookiecutter.module_name }}",
  "version": "0.1.0",
  "bin": {
    "data-pipeline": "bin/{{ cookiecutter.module_name }}.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "format": "eslint --max-warnings=0 --fix --cache bin lib && prettier --write --cache .",
    "lint": "eslint --max-warnings=0 --cache bin lib && prettier --list-different --cache ."
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/jest": "^29.5.14",
    "@types/node": "22.7.9",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "aws-cdk": "2.177.0",
    "cdk-nag": "^2.28.195",
    "fs-extra": "^11.2.0",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.177.0",
    "constructs": "^10.0.0"
  }
}
